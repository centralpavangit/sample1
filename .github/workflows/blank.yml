name: practice
on:
  push:
    branch: master
  pull_request:
    branch: master
jobs:
  mypractice:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: echo "hello world"
    
